# Assignment operators used to assign or update a value inside a variable

# =	    x = 5	x = 5           #
# +=	x += 3	x = x + 3       #increment the value
# -=	x -= 3	x = x - 3       #decrement the value
# *=	x *= 3	x = x * 3       #muliply the value
# /=	x /= 3	x = x / 3       #division the value and print float
# %=	x %= 3	x = x % 3       #"modulus" division of remainder number (vhagsesh)
# //=	x //= 3	x = x // 3      #"floor division" division of  int number
# **=	x **= 3	x = x ** 3      #"exponentiation" to print the power


a=5-3           #Assign 5-3 in a
print (a)
b=6
# b +=3 #increment the value of b by 3 and then assign it to b
b -=2 #decrement the value of b by 2 and then assign it to b
print(b)
